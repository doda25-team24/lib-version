#lib-version
A version-aware Maven library used by the App Service to demonstrate automated release engineering and runtime version retrieval.

Provides a `VersionUtil` class to retrieve the library's version programmatically without parsing Git tags.

